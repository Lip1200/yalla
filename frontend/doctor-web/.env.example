# Copy to .env.local and adjust as needed (.env.local is gitignored by Vite).

# Base URL of the FastAPI backend.
# Defaults to http://127.0.0.1:8001 if unset (matches the standard local dev port).
VITE_API_BASE_URL=http://127.0.0.1:8001
